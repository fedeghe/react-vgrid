import { createUseStyles } from "react-jss";

export default createUseStyles({
    Line: {
        display:'flex',
        justifyContent:'space-between',
        alignItems: 'center',
        padding:'10px',
        lineHeight:'1em'
    },
});